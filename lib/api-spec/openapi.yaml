openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Spanish Tutor Booking Platform API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: public
    description: Public endpoints (no auth required)
  - name: student
    description: Student endpoints (Clerk auth required)
  - name: admin
    description: Admin endpoints (password session required)
  - name: uploads
    description: File upload/download endpoints shared by admin and student
  - name: internal
    description: Internal endpoints for scheduled jobs (shared-secret header, not a user session)

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # ─── INTERNAL (scheduled jobs) ───────────────────────────────────────────
  /internal/homework-reminders/run:
    post:
      operationId: runHomeworkReminderSweep
      tags: [internal]
      summary: Sweep for unsubmitted homework past its reminder due date and flag it
      description: Guarded by an X-Internal-Secret header (compared against INTERNAL_CRON_SECRET), not a user session. Intended to be hit hourly by a Replit Scheduled Deployment.
      responses:
        "200":
          description: Sweep result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HomeworkReminderSweepResult"
        "401":
          description: Missing or invalid X-Internal-Secret header

  # ─── PUBLIC ───────────────────────────────────────────────────────────────
  /lesson-types:
    get:
      operationId: listLessonTypes
      tags: [public]
      summary: List active lesson types
      responses:
        "200":
          description: List of lesson types
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LessonType"

  /credit-bundles:
    get:
      operationId: listCreditBundles
      tags: [public]
      summary: List active credit bundles
      responses:
        "200":
          description: List of active credit bundles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CreditBundle"

  /available-slots:
    get:
      operationId: getAvailableSlots
      tags: [public]
      summary: Get available booking slots
      parameters:
        - name: lessonTypeId
          in: query
          required: true
          schema:
            type: number
        - name: startDate
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          required: true
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Available time slots
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TimeSlot"

  /testimonials:
    get:
      operationId: listTestimonials
      tags: [public]
      summary: List visible testimonials
      responses:
        "200":
          description: List of testimonials
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Testimonial"

  /faqs:
    get:
      operationId: listFaqs
      tags: [public]
      summary: List visible FAQ entries
      responses:
        "200":
          description: List of FAQ entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FaqEntry"

  /site-settings:
    get:
      operationId: getSiteSettings
      tags: [public]
      summary: Get public site settings
      responses:
        "200":
          description: Site settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettings"

  # ─── STUDENT ──────────────────────────────────────────────────────────────
  /student/me:
    get:
      operationId: getStudentProfile
      tags: [student]
      summary: Get current student profile and credit summary
      responses:
        "200":
          description: Student profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentProfile"
        "401":
          description: Unauthorized

  /student/dashboard:
    get:
      operationId: getStudentDashboard
      tags: [student]
      summary: Get student dashboard summary
      responses:
        "200":
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StudentDashboard"
        "401":
          description: Unauthorized

  /student/bookings:
    get:
      operationId: listStudentBookings
      tags: [student]
      summary: List student's bookings
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [upcoming, past, cancelled]
      responses:
        "200":
          description: List of bookings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Booking"
        "401":
          description: Unauthorized
    post:
      operationId: createBooking
      tags: [student]
      summary: Book a lesson
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BookingInput"
      responses:
        "201":
          description: Booking created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "400":
          description: Invalid input or no credits
        "401":
          description: Unauthorized

  /student/bookings/{id}:
    get:
      operationId: getStudentBooking
      tags: [student]
      summary: Get booking details
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Booking details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookingDetail"
        "404":
          description: Not found

  /student/bookings/{id}/cancel:
    patch:
      operationId: cancelBooking
      tags: [student]
      summary: Cancel a booking
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancellationInput"
      responses:
        "200":
          description: Booking cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "400":
          description: Cannot cancel
        "404":
          description: Not found

  /student/bookings/{id}/reschedule:
    patch:
      operationId: rescheduleBooking
      tags: [student]
      summary: Reschedule a booking to a new time
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RescheduleInput"
      responses:
        "200":
          description: Booking rescheduled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "400":
          description: Cannot reschedule
        "404":
          description: Not found

  /student/bookings/{id}/homework:
    get:
      operationId: getHomework
      tags: [student]
      summary: Get homework for a booking
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Homework entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Homework"
        "404":
          description: Not found
    post:
      operationId: submitHomework
      tags: [student]
      summary: Submit homework for a booking
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HomeworkInput"
      responses:
        "201":
          description: Homework submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Homework"
        "400":
          description: Invalid input
        "404":
          description: Not found

  /student/homework:
    get:
      operationId: listStudentHomework
      tags: [student]
      summary: List all homework for the logged-in student across all bookings
      responses:
        "200":
          description: All homework for the current student
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminHomework"

  /student/test-homework:
    get:
      operationId: listStudentTestHomework
      tags: [student]
      summary: "[Sandbox] List all test homework rows"
      responses:
        "200":
          description: All test homework
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TestHomework"

  /student/test-homework/{id}/submit:
    post:
      operationId: submitTestHomework
      tags: [student]
      summary: "[Sandbox] Submit a test homework row"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestHomeworkSubmitInput"
      responses:
        "200":
          description: Test homework submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestHomework"
        "404":
          description: Not found

  /student/bookings/{id}/review:
    post:
      operationId: submitReview
      tags: [student]
      summary: Submit a review for a completed lesson
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewInput"
      responses:
        "201":
          description: Review submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Review"
        "400":
          description: Invalid input
        "404":
          description: Not found

  /student/packages:
    get:
      operationId: listStudentPackages
      tags: [student]
      summary: List student's lesson packages and credits
      responses:
        "200":
          description: Lesson packages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LessonPackage"
        "401":
          description: Unauthorized

  /student/tour-complete:
    patch:
      operationId: completeTour
      tags: [student]
      summary: Mark the onboarding tour as seen for the current student
      responses:
        "200":
          description: Tour marked complete
          content:
            application/json:
              schema:
                type: object
                properties:
                  hasSeenTour:
                    type: boolean
                required:
                  - hasSeenTour
        "401":
          description: Unauthorized

  /student/messages:
    get:
      operationId: listStudentMessages
      tags: [student]
      summary: Get the student's message thread with the teacher
      responses:
        "200":
          description: Messages, oldest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"
        "401":
          description: Unauthorized
    post:
      operationId: sendStudentMessage
      tags: [student]
      summary: Send a message to the teacher
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageInput"
      responses:
        "201":
          description: Message sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"
        "401":
          description: Unauthorized

  # ─── ADMIN ────────────────────────────────────────────────────────────────
  /admin/login:
    post:
      operationId: adminLogin
      tags: [admin]
      summary: Admin login with password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminLoginInput"
      responses:
        "200":
          description: Logged in
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminAuthResult"
        "401":
          description: Invalid password

  /admin/logout:
    post:
      operationId: adminLogout
      tags: [admin]
      summary: Admin logout
      responses:
        "200":
          description: Logged out

  /admin/dashboard:
    get:
      operationId: getAdminDashboard
      tags: [admin]
      summary: Get admin dashboard summary
      responses:
        "200":
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminDashboard"
        "401":
          description: Unauthorized

  /admin/bookings:
    get:
      operationId: listAdminBookings
      tags: [admin]
      summary: List all bookings
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
        - name: date
          in: query
          required: false
          schema:
            type: string
            format: date
      responses:
        "200":
          description: All bookings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminBooking"
        "401":
          description: Unauthorized

  /admin/bookings/{id}:
    patch:
      operationId: updateAdminBooking
      tags: [admin]
      summary: Update a booking status or notes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminBookingUpdate"
      responses:
        "200":
          description: Updated booking
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminBooking"
        "404":
          description: Not found

  /admin/bookings/{id}/complete:
    patch:
      operationId: completeBooking
      tags: [admin]
      summary: Complete a lesson with a recap and optional homework assignment
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteBookingInput"
      responses:
        "200":
          description: Booking marked completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminBooking"
        "400":
          description: Invalid input
        "404":
          description: Not found

  /admin/lesson-types:
    get:
      operationId: listAdminLessonTypes
      tags: [admin]
      summary: List all lesson types (including inactive)
      responses:
        "200":
          description: All lesson types
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LessonType"
    post:
      operationId: createLessonType
      tags: [admin]
      summary: Create a new lesson type
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LessonTypeInput"
      responses:
        "201":
          description: Created lesson type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonType"

  /admin/lesson-types/{id}:
    patch:
      operationId: updateLessonType
      tags: [admin]
      summary: Update a lesson type
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LessonTypeUpdate"
      responses:
        "200":
          description: Updated lesson type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonType"
        "404":
          description: Not found
    delete:
      operationId: deleteLessonType
      tags: [admin]
      summary: Delete a lesson type
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted

  /admin/credit-bundles:
    get:
      operationId: listAdminCreditBundles
      tags: [admin]
      summary: List all credit bundles (including inactive)
      responses:
        "200":
          description: All credit bundles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CreditBundle"
    post:
      operationId: createCreditBundle
      tags: [admin]
      summary: Create a purchasable credit bundle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreditBundleInput"
      responses:
        "201":
          description: Created credit bundle
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditBundle"

  /admin/credit-bundles/{id}:
    patch:
      operationId: updateCreditBundle
      tags: [admin]
      summary: Update a credit bundle
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreditBundleUpdate"
      responses:
        "200":
          description: Updated credit bundle
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditBundle"
        "404":
          description: Not found
    delete:
      operationId: deleteCreditBundle
      tags: [admin]
      summary: Delete a credit bundle
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted

  /admin/homework:
    get:
      operationId: listAdminHomework
      tags: [admin]
      summary: List all homework submissions
      parameters:
        - name: reviewed
          in: query
          required: false
          schema:
            type: boolean
        - name: studentId
          in: query
          required: false
          schema:
            type: number
        - name: submitted
          in: query
          required: false
          description: Defaults to true (existing behavior — only submitted homework). Set to false to include assigned-but-unsubmitted homework too.
          schema:
            type: boolean
      responses:
        "200":
          description: All homework
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminHomework"
        "401":
          description: Unauthorized

  /admin/homework/{id}:
    patch:
      operationId: updateHomework
      tags: [admin]
      summary: Add tutor feedback and grade to homework
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HomeworkFeedbackInput"
      responses:
        "200":
          description: Updated homework
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminHomework"
        "404":
          description: Not found

  /admin/test-homework:
    post:
      operationId: createTestHomework
      tags: [admin]
      summary: "[Sandbox] Create/assign a test homework row"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestHomeworkCreateInput"
      responses:
        "201":
          description: Test homework created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestHomework"
        "401":
          description: Unauthorized
    get:
      operationId: listAdminTestHomework
      tags: [admin]
      summary: "[Sandbox] List all test homework rows"
      parameters:
        - name: reviewed
          in: query
          required: false
          schema:
            type: boolean
        - name: submitted
          in: query
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: All test homework
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TestHomework"
        "401":
          description: Unauthorized

  /admin/test-homework/{id}:
    patch:
      operationId: updateTestHomework
      tags: [admin]
      summary: "[Sandbox] Edit assignment or add tutor feedback/grade to test homework"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestHomeworkUpdateInput"
      responses:
        "200":
          description: Updated test homework
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestHomework"
        "404":
          description: Not found
    delete:
      operationId: deleteTestHomework
      tags: [admin]
      summary: "[Sandbox] Delete a test homework row"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  /admin/students:
    get:
      operationId: listAdminStudents
      tags: [admin]
      summary: List all students
      responses:
        "200":
          description: All students
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminStudent"
        "401":
          description: Unauthorized

  /admin/students/{id}:
    get:
      operationId: getAdminStudent
      tags: [admin]
      summary: Get student details with history
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Student detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminStudentDetail"
        "404":
          description: Not found

  /admin/messages:
    get:
      operationId: listAdminMessageThreads
      tags: [admin]
      summary: List message threads (one per student), most recent first
      responses:
        "200":
          description: Message threads
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MessageThread"
        "401":
          description: Unauthorized

  /admin/students/{id}/messages:
    get:
      operationId: listAdminStudentMessages
      tags: [admin]
      summary: Get the message thread with a specific student
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Messages, oldest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    post:
      operationId: sendAdminMessage
      tags: [admin]
      summary: Send a message to a student
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageInput"
      responses:
        "201":
          description: Message sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /admin/packages:
    post:
      operationId: grantPackage
      tags: [admin]
      summary: Grant a lesson package to a student
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PackageGrantInput"
      responses:
        "201":
          description: Package granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LessonPackage"
        "400":
          description: Invalid input

  /admin/testimonials:
    get:
      operationId: listAdminTestimonials
      tags: [admin]
      summary: List all testimonials
      responses:
        "200":
          description: All testimonials
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Testimonial"
    post:
      operationId: createTestimonial
      tags: [admin]
      summary: Create a testimonial
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestimonialInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Testimonial"

  /admin/testimonials/{id}:
    patch:
      operationId: updateTestimonial
      tags: [admin]
      summary: Update testimonial visibility or content
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestimonialUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Testimonial"
        "404":
          description: Not found
    delete:
      operationId: deleteTestimonial
      tags: [admin]
      summary: Delete a testimonial
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted

  /admin/faqs:
    get:
      operationId: listAdminFaqs
      tags: [admin]
      summary: List all FAQ entries
      responses:
        "200":
          description: All FAQs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FaqEntry"
    post:
      operationId: createFaq
      tags: [admin]
      summary: Create a FAQ entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FaqInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FaqEntry"

  /admin/faqs/{id}:
    patch:
      operationId: updateFaq
      tags: [admin]
      summary: Update a FAQ entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FaqUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FaqEntry"
        "404":
          description: Not found
    delete:
      operationId: deleteFaq
      tags: [admin]
      summary: Delete a FAQ entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted

  /admin/site-settings:
    get:
      operationId: getAdminSiteSettings
      tags: [admin]
      summary: Get full site settings
      responses:
        "200":
          description: Site settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettings"
    patch:
      operationId: updateSiteSettings
      tags: [admin]
      summary: Update site settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteSettingsUpdate"
      responses:
        "200":
          description: Updated site settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettings"

  /admin/availability-overrides:
    get:
      operationId: listAvailabilityOverrides
      tags: [admin]
      summary: List upcoming teacher time-off blocks
      responses:
        "200":
          description: Upcoming availability overrides
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AvailabilityOverride"
        "401":
          description: Unauthorized

  /admin/availability-overrides/day:
    put:
      operationId: setDayAvailabilityOverrides
      tags: [admin]
      summary: Replace all time-off blocks for a single date
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetDayOverridesInput"
      responses:
        "200":
          description: The date's new set of overrides
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AvailabilityOverride"
        "400":
          description: Invalid input
        "401":
          description: Unauthorized

  /admin/availability-overrides/{id}:
    delete:
      operationId: deleteAvailabilityOverride
      tags: [admin]
      summary: Delete a single time-off block
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized

  /admin/calendar/busy:
    get:
      operationId: getCalendarBusy
      tags: [admin]
      summary: Get Google Calendar busy blocks for a single date
      parameters:
        - in: query
          name: date
          required: true
          schema:
            type: string
            format: date
          description: The date to query in "YYYY-MM-DD" format
      responses:
        "200":
          description: List of busy time ranges from Google Calendar
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CalendarBusyBlock"
        "401":
          description: Unauthorized

  /admin/calendar/status:
    get:
      operationId: getCalendarStatus
      tags: [admin]
      summary: Check Google Calendar connection status
      responses:
        "200":
          description: Calendar status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarStatus"

  /admin/calendar/disconnect:
    delete:
      operationId: disconnectCalendar
      tags: [admin]
      summary: Disconnect Google Calendar
      responses:
        "200":
          description: Disconnected successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                required:
                  - success

  # ─── UPLOADS ──────────────────────────────────────────────────────────────
  /uploads:
    post:
      operationId: uploadFile
      tags: [uploads]
      summary: Upload a file (PDF or image) to object storage
      description: Admin session covers homework-assigned/homework-review contexts; a Clerk-authenticated student who owns the booking covers homework-submission.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/UploadFileInput"
      responses:
        "201":
          description: Uploaded file metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadedFile"
        "400":
          description: Invalid file
        "401":
          description: Unauthorized

  /files/homework/{homeworkId}/{which}:
    get:
      operationId: getHomeworkFile
      tags: [uploads]
      summary: Download/view a homework file (assigned, submission, or review) via a private proxy
      parameters:
        - name: homeworkId
          in: path
          required: true
          schema:
            type: number
        - name: which
          in: path
          required: true
          schema:
            type: string
            enum: [assigned, submission, review]
      responses:
        "200":
          description: File contents
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /files/test-homework/{id}/{which}:
    get:
      operationId: getTestHomeworkFile
      tags: [uploads]
      summary: "[Sandbox] Download/view a test homework file (assigned, submission, or review) via a private proxy"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: number
        - name: which
          in: path
          required: true
          schema:
            type: string
            enum: [assigned, submission, review]
      responses:
        "200":
          description: File contents
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "401":
          description: Unauthorized
        "404":
          description: Not found

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    LessonType:
      type: object
      properties:
        id:
          type: number
        name:
          type: string
        durationMinutes:
          type: number
        creditCost:
          type: number
        description:
          type: string
        isActive:
          type: boolean
        isTrial:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - durationMinutes
        - creditCost
        - description
        - isActive
        - isTrial
        - createdAt

    LessonTypeInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        durationMinutes:
          type: number
          minimum: 15
        creditCost:
          type: number
          minimum: 1
        description:
          type: string
        isActive:
          type: boolean
        isTrial:
          type: boolean
      required:
        - name
        - durationMinutes
        - creditCost
        - description

    LessonTypeUpdate:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        durationMinutes:
          type: number
          minimum: 15
        creditCost:
          type: number
          minimum: 1
        description:
          type: string
        isActive:
          type: boolean
        isTrial:
          type: boolean

    CreditBundle:
      type: object
      properties:
        id:
          type: number
        credits:
          type: number
        priceCents:
          type: number
        sortOrder:
          type: number
        isActive:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - credits
        - priceCents
        - sortOrder
        - isActive
        - createdAt

    CreditBundleInput:
      type: object
      properties:
        credits:
          type: number
          minimum: 1
        priceCents:
          type: number
          minimum: 0
        sortOrder:
          type: number
        isActive:
          type: boolean
      required:
        - credits
        - priceCents

    CreditBundleUpdate:
      type: object
      properties:
        credits:
          type: number
          minimum: 1
        priceCents:
          type: number
          minimum: 0
        sortOrder:
          type: number
        isActive:
          type: boolean

    TimeSlot:
      type: object
      properties:
        startTime:
          type: string
          format: date-time
        endTime:
          type: string
          format: date-time
        available:
          type: boolean
          description: False when this slot overlaps a busy block (Google Calendar, a teacher time-off block, or an existing booking); the UI greys it out.
      required:
        - startTime
        - endTime
        - available

    CalendarBusyBlock:
      type: object
      required: [start, end]
      properties:
        start:
          type: string
          format: date-time
        end:
          type: string
          format: date-time

    AvailabilityOverride:
      type: object
      properties:
        id:
          type: number
        startTime:
          type: string
          format: date-time
        endTime:
          type: string
          format: date-time
      required:
        - id
        - startTime
        - endTime

    SetDayOverridesInput:
      type: object
      properties:
        date:
          type: string
          description: The date whose overrides are being replaced, as "YYYY-MM-DD".
        blocks:
          type: array
          items:
            type: object
            properties:
              startTime:
                type: string
                format: date-time
              endTime:
                type: string
                format: date-time
            required:
              - startTime
              - endTime
      required:
        - date
        - blocks

    Booking:
      type: object
      properties:
        id:
          type: number
        lessonTypeId:
          type: number
        lessonTypeName:
          type: string
        startTime:
          type: string
          format: date-time
        endTime:
          type: string
          format: date-time
        status:
          type: string
          enum: [upcoming, completed, cancelled]
        meetLink:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - lessonTypeId
        - lessonTypeName
        - startTime
        - endTime
        - status
        - meetLink
        - createdAt

    BookingDetail:
      type: object
      properties:
        id:
          type: number
        lessonTypeId:
          type: number
        lessonTypeName:
          type: string
        durationMinutes:
          type: number
        startTime:
          type: string
          format: date-time
        endTime:
          type: string
          format: date-time
        status:
          type: string
          enum: [upcoming, completed, cancelled]
        meetLink:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
          description: The teacher's recap of the lesson, set when the booking is completed.
        homework:
          $ref: "#/components/schemas/Homework"
        review:
          $ref: "#/components/schemas/Review"
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - lessonTypeId
        - lessonTypeName
        - durationMinutes
        - startTime
        - endTime
        - status
        - meetLink
        - notes
        - createdAt

    BookingInput:
      type: object
      properties:
        lessonTypeId:
          type: number
        startTime:
          type: string
          format: date-time
      required:
        - lessonTypeId
        - startTime

    CancellationInput:
      type: object
      properties:
        reason:
          type: string

    RescheduleInput:
      type: object
      properties:
        newStartTime:
          type: string
          format: date-time
      required:
        - newStartTime

    Homework:
      type: object
      properties:
        id:
          type: number
        bookingId:
          type: number
        assignedText:
          type: ["string", "null"]
        assignedFileUrl:
          type: ["string", "null"]
        assignedFileKey:
          type: ["string", "null"]
        assignedFileName:
          type: ["string", "null"]
        assignedFileMime:
          type: ["string", "null"]
        submittedText:
          type: ["string", "null"]
        fileUrl:
          type: ["string", "null"]
        submittedFileKey:
          type: ["string", "null"]
        submittedFileName:
          type: ["string", "null"]
        submittedFileMime:
          type: ["string", "null"]
        reviewedFileKey:
          type: ["string", "null"]
        reviewedFileName:
          type: ["string", "null"]
        reviewedFileMime:
          type: ["string", "null"]
        tutorFeedback:
          type: ["string", "null"]
        grade:
          type: ["string", "null"]
        submittedAt:
          type: ["string", "null"]
          format: date-time
        reviewedAt:
          type: ["string", "null"]
          format: date-time
        reminderActive:
          type: boolean
      required:
        - id
        - bookingId
        - assignedText
        - assignedFileUrl
        - assignedFileKey
        - assignedFileName
        - assignedFileMime
        - submittedText
        - fileUrl
        - submittedFileKey
        - submittedFileName
        - submittedFileMime
        - reviewedFileKey
        - reviewedFileName
        - reviewedFileMime
        - tutorFeedback
        - grade
        - submittedAt
        - reviewedAt
        - reminderActive

    HomeworkInput:
      type: object
      properties:
        submittedText:
          type: string
        fileUrl:
          type: string
        fileKey:
          type: string
        fileName:
          type: string
        fileMime:
          type: string

    HomeworkFeedbackInput:
      type: object
      properties:
        tutorFeedback:
          type: string
        grade:
          type: string
        reviewedFileKey:
          type: string
        reviewedFileName:
          type: string
        reviewedFileMime:
          type: string

    TestHomework:
      type: object
      properties:
        id:
          type: number
        assignedText:
          type: ["string", "null"]
        assignedFileUrl:
          type: ["string", "null"]
        assignedFileKey:
          type: ["string", "null"]
        assignedFileName:
          type: ["string", "null"]
        assignedFileMime:
          type: ["string", "null"]
        submittedText:
          type: ["string", "null"]
        fileUrl:
          type: ["string", "null"]
        submittedFileKey:
          type: ["string", "null"]
        submittedFileName:
          type: ["string", "null"]
        submittedFileMime:
          type: ["string", "null"]
        reviewedFileKey:
          type: ["string", "null"]
        reviewedFileName:
          type: ["string", "null"]
        reviewedFileMime:
          type: ["string", "null"]
        tutorFeedback:
          type: ["string", "null"]
        grade:
          type: ["string", "null"]
        submittedAt:
          type: ["string", "null"]
          format: date-time
        reviewedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - assignedText
        - assignedFileUrl
        - assignedFileKey
        - assignedFileName
        - assignedFileMime
        - submittedText
        - fileUrl
        - submittedFileKey
        - submittedFileName
        - submittedFileMime
        - reviewedFileKey
        - reviewedFileName
        - reviewedFileMime
        - tutorFeedback
        - grade
        - submittedAt
        - reviewedAt
        - createdAt

    TestHomeworkCreateInput:
      type: object
      properties:
        assignedText:
          type: string
        assignedFileUrl:
          type: string
        assignedFileKey:
          type: string
        assignedFileName:
          type: string
        assignedFileMime:
          type: string

    TestHomeworkUpdateInput:
      type: object
      description: Dual-purpose — re-editing the assignment and/or grading the submission. All fields optional.
      properties:
        assignedText:
          type: string
        assignedFileUrl:
          type: string
        assignedFileKey:
          type: string
        assignedFileName:
          type: string
        assignedFileMime:
          type: string
        tutorFeedback:
          type: string
        grade:
          type: string
        reviewedFileKey:
          type: string
        reviewedFileName:
          type: string
        reviewedFileMime:
          type: string

    TestHomeworkSubmitInput:
      type: object
      properties:
        submittedText:
          type: string
        fileUrl:
          type: string
        fileKey:
          type: string
        fileName:
          type: string
        fileMime:
          type: string

    UploadedFile:
      type: object
      properties:
        key:
          type: string
        fileName:
          type: string
        mimeType:
          type: string
        size:
          type: number
      required:
        - key
        - fileName
        - mimeType
        - size

    UploadFileInput:
      type: object
      description: Multipart form fields for POST /uploads. The "file" field carries the binary upload; multer parses it server-side rather than this schema.
      properties:
        file:
          type: string
        context:
          type: string
          enum: [homework-assigned, homework-submission, homework-review, test-homework-assigned, test-homework-submission, test-homework-review]
        bookingId:
          type: number
      required:
        - file
        - context
        - bookingId

    HomeworkReminderSweepResult:
      type: object
      properties:
        checkedAt:
          type: string
          format: date-time
        remindersSet:
          type: number
        homeworkIds:
          type: array
          items:
            type: number
      required:
        - checkedAt
        - remindersSet
        - homeworkIds

    Review:
      type: object
      properties:
        id:
          type: number
        bookingId:
          type: number
        rating:
          type: number
          minimum: 1
          maximum: 5
        comment:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - bookingId
        - rating
        - comment
        - createdAt

    ReviewInput:
      type: object
      properties:
        rating:
          type: number
          minimum: 1
          maximum: 5
        comment:
          type: string
      required:
        - rating

    LessonPackage:
      type: object
      properties:
        id:
          type: number
        totalCredits:
          type: number
        usedCredits:
          type: number
        remainingCredits:
          type: number
        purchasedAt:
          type: string
          format: date-time
      required:
        - id
        - totalCredits
        - usedCredits
        - remainingCredits
        - purchasedAt

    StudentProfile:
      type: object
      properties:
        id:
          type: number
        clerkUserId:
          type: string
        email:
          type: string
        displayName:
          type: string
        totalRemainingCredits:
          type: number
        upcomingLessonsCount:
          type: number
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - clerkUserId
        - email
        - displayName
        - totalRemainingCredits
        - upcomingLessonsCount
        - createdAt

    StudentDashboard:
      type: object
      properties:
        nextBooking:
          $ref: "#/components/schemas/Booking"
        upcomingBookings:
          type: array
          items:
            $ref: "#/components/schemas/Booking"
        totalRemainingCredits:
          type: number
        trialAvailable:
          type: boolean
          description: Whether this student can still book the free trial lesson (never booked it before, and one is configured/enabled).
        hasSeenTour:
          type: boolean
          description: Whether this student has already seen (or skipped) the first-login onboarding tour.
        pendingHomeworkCount:
          type: number
        packages:
          type: array
          items:
            $ref: "#/components/schemas/LessonPackage"
        recentHomework:
          type: array
          items:
            $ref: "#/components/schemas/AdminHomework"
      required:
        - upcomingBookings
        - totalRemainingCredits
        - trialAvailable
        - hasSeenTour
        - pendingHomeworkCount
        - packages
        - recentHomework

    Testimonial:
      type: object
      properties:
        id:
          type: number
        studentName:
          type: string
        text:
          type: string
        rating:
          type: number
          minimum: 1
          maximum: 5
        isVisible:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - studentName
        - text
        - rating
        - isVisible
        - createdAt

    TestimonialInput:
      type: object
      properties:
        studentName:
          type: string
          minLength: 1
        text:
          type: string
          minLength: 1
        rating:
          type: number
          minimum: 1
          maximum: 5
        isVisible:
          type: boolean
      required:
        - studentName
        - text
        - rating

    TestimonialUpdate:
      type: object
      properties:
        studentName:
          type: string
        text:
          type: string
        rating:
          type: number
          minimum: 1
          maximum: 5
        isVisible:
          type: boolean

    FaqEntry:
      type: object
      properties:
        id:
          type: number
        question:
          type: string
        answer:
          type: string
        displayOrder:
          type: number
        isVisible:
          type: boolean
      required:
        - id
        - question
        - answer
        - displayOrder
        - isVisible

    FaqInput:
      type: object
      properties:
        question:
          type: string
          minLength: 1
        answer:
          type: string
          minLength: 1
        displayOrder:
          type: number
        isVisible:
          type: boolean
      required:
        - question
        - answer

    FaqUpdate:
      type: object
      properties:
        question:
          type: string
        answer:
          type: string
        displayOrder:
          type: number
        isVisible:
          type: boolean

    DayHours:
      type: object
      properties:
        enabled:
          type: boolean
        start:
          type: string
          description: 24h "HH:mm"
        end:
          type: string
          description: 24h "HH:mm"
      required:
        - enabled
        - start
        - end

    WeeklyHours:
      type: object
      properties:
        mon:
          $ref: "#/components/schemas/DayHours"
        tue:
          $ref: "#/components/schemas/DayHours"
        wed:
          $ref: "#/components/schemas/DayHours"
        thu:
          $ref: "#/components/schemas/DayHours"
        fri:
          $ref: "#/components/schemas/DayHours"
        sat:
          $ref: "#/components/schemas/DayHours"
        sun:
          $ref: "#/components/schemas/DayHours"
      required:
        - mon
        - tue
        - wed
        - thu
        - fri
        - sat
        - sun

    SiteSettings:
      type: object
      properties:
        id:
          type: number
        tutorName:
          type: string
        tutorBio:
          type: string
        contactEmail:
          type: string
        freeTrialEnabled:
          type: boolean
        tutorPhotoUrl:
          type: ["string", "null"]
        weeklyHours:
          $ref: "#/components/schemas/WeeklyHours"
        timezone:
          type: string
          description: IANA timezone the tutor's weekly hours are expressed in (e.g. "Europe/Madrid").
      required:
        - id
        - tutorName
        - tutorBio
        - contactEmail
        - freeTrialEnabled
        - tutorPhotoUrl
        - weeklyHours
        - timezone

    SiteSettingsUpdate:
      type: object
      properties:
        tutorName:
          type: string
        tutorBio:
          type: string
        contactEmail:
          type: string
        freeTrialEnabled:
          type: boolean
        tutorPhotoUrl:
          type: string
        weeklyHours:
          $ref: "#/components/schemas/WeeklyHours"
        timezone:
          type: string

    AdminLoginInput:
      type: object
      properties:
        password:
          type: string
      required:
        - password

    AdminAuthResult:
      type: object
      properties:
        success:
          type: boolean
      required:
        - success

    AdminDashboard:
      type: object
      properties:
        todayBookings:
          type: array
          items:
            $ref: "#/components/schemas/AdminBooking"
        upcomingBookingsCount:
          type: number
        totalStudents:
          type: number
        pendingHomeworkCount:
          type: number
        pendingTestimonialsCount:
          type: number
        thisWeekBookings:
          type: number
      required:
        - todayBookings
        - upcomingBookingsCount
        - totalStudents
        - pendingHomeworkCount
        - pendingTestimonialsCount
        - thisWeekBookings

    AdminBooking:
      type: object
      properties:
        id:
          type: number
        studentId:
          type: number
        studentName:
          type: string
        studentEmail:
          type: string
        lessonTypeId:
          type: number
        lessonTypeName:
          type: string
        startTime:
          type: string
          format: date-time
        endTime:
          type: string
          format: date-time
        status:
          type: string
          enum: [upcoming, completed, cancelled]
        meetLink:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - studentId
        - studentName
        - studentEmail
        - lessonTypeId
        - lessonTypeName
        - startTime
        - endTime
        - status
        - meetLink
        - notes
        - createdAt

    AdminBookingUpdate:
      type: object
      properties:
        status:
          type: string
          enum: [upcoming, cancelled]
        notes:
          type: string

    CompleteBookingInput:
      type: object
      properties:
        notes:
          type: string
          minLength: 1
          description: The teacher's recap of the lesson.
        homeworkAssignedText:
          type: string
        homeworkAssignedFileUrl:
          type: string
        homeworkAssignedFileKey:
          type: string
        homeworkAssignedFileName:
          type: string
        homeworkAssignedFileMime:
          type: string
      required:
        - notes

    AdminHomework:
      type: object
      properties:
        id:
          type: number
        bookingId:
          type: number
        studentId:
          type: number
        studentName:
          type: string
        lessonTypeName:
          type: string
        lessonDate:
          type: string
          format: date-time
        assignedText:
          type: ["string", "null"]
        assignedFileUrl:
          type: ["string", "null"]
        assignedFileKey:
          type: ["string", "null"]
        assignedFileName:
          type: ["string", "null"]
        assignedFileMime:
          type: ["string", "null"]
        submittedText:
          type: ["string", "null"]
        fileUrl:
          type: ["string", "null"]
        submittedFileKey:
          type: ["string", "null"]
        submittedFileName:
          type: ["string", "null"]
        submittedFileMime:
          type: ["string", "null"]
        reviewedFileKey:
          type: ["string", "null"]
        reviewedFileName:
          type: ["string", "null"]
        reviewedFileMime:
          type: ["string", "null"]
        tutorFeedback:
          type: ["string", "null"]
        grade:
          type: ["string", "null"]
        submittedAt:
          type: ["string", "null"]
          format: date-time
        reviewedAt:
          type: ["string", "null"]
          format: date-time
        reminderActive:
          type: boolean
      required:
        - id
        - bookingId
        - studentId
        - studentName
        - lessonTypeName
        - lessonDate
        - assignedText
        - assignedFileUrl
        - assignedFileKey
        - assignedFileName
        - assignedFileMime
        - submittedText
        - fileUrl
        - submittedFileKey
        - submittedFileName
        - submittedFileMime
        - reviewedFileKey
        - reviewedFileName
        - reviewedFileMime
        - tutorFeedback
        - grade
        - submittedAt
        - reviewedAt
        - reminderActive

    AdminStudent:
      type: object
      properties:
        id:
          type: number
        email:
          type: string
        displayName:
          type: string
        totalCredits:
          type: number
        usedCredits:
          type: number
        totalBookings:
          type: number
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - email
        - displayName
        - totalCredits
        - usedCredits
        - totalBookings
        - createdAt

    Message:
      type: object
      properties:
        id:
          type: number
        studentId:
          type: number
        senderRole:
          type: string
          enum: [student, admin]
        body:
          type: string
        createdAt:
          type: string
          format: date-time
        readAt:
          type: ["string", "null"]
          format: date-time
      required:
        - id
        - studentId
        - senderRole
        - body
        - createdAt
        - readAt

    MessageInput:
      type: object
      properties:
        body:
          type: string
          minLength: 1
      required:
        - body

    MessageThread:
      type: object
      properties:
        studentId:
          type: number
        studentName:
          type: string
        studentEmail:
          type: string
        lastMessageBody:
          type: string
        lastMessageAt:
          type: string
          format: date-time
        unreadCount:
          type: number
      required:
        - studentId
        - studentName
        - studentEmail
        - lastMessageBody
        - lastMessageAt
        - unreadCount

    AdminStudentDetail:
      type: object
      properties:
        id:
          type: number
        email:
          type: string
        displayName:
          type: string
        packages:
          type: array
          items:
            $ref: "#/components/schemas/LessonPackage"
        bookings:
          type: array
          items:
            $ref: "#/components/schemas/Booking"
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - email
        - displayName
        - packages
        - bookings
        - createdAt

    PackageGrantInput:
      type: object
      properties:
        studentId:
          type: number
        totalCredits:
          type: number
          minimum: 1
      required:
        - studentId
        - totalCredits

    CalendarStatus:
      type: object
      properties:
        connected:
          type: boolean
        calendarEmail:
          type: ["string", "null"]
      required:
        - connected
        - calendarEmail
